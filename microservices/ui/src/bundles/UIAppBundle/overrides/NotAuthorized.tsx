import * as React from "react";
import { Components } from "@kaviar/x-ui";

export function NotAuthorized(props: Components.NotAuthorizedProps) {
  return (
    <div>
      <h1>You are not currently authorised to view this page</h1>
    </div>
  );
}
